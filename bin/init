#!/usr/bin/env bash

# Download repo archive
curl -L https://github.com/tribes-protocol/agent/archive/refs/heads/main.zip -o repo.zip

# Extract and rename
unzip repo.zip
mv agent-main agent

# Cleanup
rm repo.zip
